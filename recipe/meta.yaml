{% set name = "zabbix_utils" %}
{% set version = "2.0.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/zabbix_utils-{{ version }}.tar.gz
  sha256: 2fdb3873bff1151b744a9cd5664522d398782cdd3a56cc6b998179f5c989e78a

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.8
    - pip
  run:
    - python >=3.8

test:
  imports:
    - zabbix_utils
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/zabbix/python-zabbix-utils
  summary: A library with modules for working with Zabbix (Zabbix API, Zabbix sender, Zabbix get)
  dev_url: https://github.com/zabbix/python-zabbix-utils
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - cousinbr
